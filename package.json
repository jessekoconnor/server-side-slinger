{
  "name": "server-side-slinger",
  "version": "1.0.0",
  "description": "This is a sample template for sam-app - Below is a brief explanation of what we have generated for you:",
  "main": "index.js",
  "scripts": {
    "start-local": "TABLE_NAME=JesseTest DISABLE_WIDGET_CACHE=true LOCAL_LAMBDA=true sam local start-api",
    "start-local-lambda": "sam local start-lambda",
    "tdd": "cd src && npm run tdd && cd ../",
    "test": "cd src && npm install && npm test && cd ../",
    "integration-test": "cd src && npm run integration-test && cd ../",
    "sam-validate": "sam validate -t template.yaml",
    "sam-package": "sam package --template-file template.yaml --output-template-file packaged.yaml --s3-bucket dashmobile-deploy",
    "sam-deploy": "sam deploy --template-file packaged.yaml --stack-name ServerSideScraper --capabilities CAPABILITY_IAM",
    "deploy-code": "npm run sam-validate && npm run sam-package && npm run sam-deploy",
    "deploy-images": "cd img && ./deploy.sh && cd ../"
  },
  "author": "",
  "license": "ISC"
}
